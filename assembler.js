load("lib/io.js");

Assembler = {};

Assembler.assemble = function () {
  var input = IO.read();

  ///// Implementation pending.
  var output="01010101010101";
  /////

  IO.write(output);
};

Assembler.assemble();
